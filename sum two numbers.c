#include <stdio.h>
int main(){
	// code to sum two numbers
	int a, b, sum;
	printf("enter two numbers \n");
	scanf("%d %d", &a, &b);
	sum = a + b;
	printf("the sum of the two numbers is %d", sum); 
	
	return 0;
}
